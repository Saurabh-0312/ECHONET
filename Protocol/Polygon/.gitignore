# Compiler files
cache/
out/

# Ignores development broadcast logs
!/broadcast
/broadcast/*/31337/
/broadcast/**/dry-run/

# Docs
docs/
./lib

# Ignore vendor libraries we don't want in git. Prefer adding them to the
# project's package management (submodule, package.json, or remappings) instead
# of committing the full sources. Keep `lib/openzeppelin-contracts/` ignored.
lib/openzeppelin-contracts/

# Dotenv file
.env
